# Dependencies
node_modules/

# Compiled native helpers (built from .swift source via build/compile-mac-helpers.sh
# in CI, or locally before first dev run on macOS)
electron/helpers/ocr-vision
electron/helpers/scroll-helper
electron/helpers/window-at-point

# Build outputs
out/
dist/
dist-electron/

# electron-vite cache
.vite/

# electron-builder artifacts
release/
*.exe
*.dmg
*.AppImage
*.deb
*.rpm
*.snap
*.nsis

# Logs
logs/
*.log
npm-debug.log*

# Runtime / OS
.DS_Store
Thumbs.db
*.swp
*.swo

# Environment & secrets
.env
.env.local
.env.*.local
electron/gdrive-credentials.ts

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript cache
*.tsbuildinfo

# electron-store data (user data, not source)
# (stored in app.getPath('userData'), not in repo)

# Claude Code local user settings (machine-specific, not shared)
.claude/settings.local.json
